# 2025.07.02: 0.1.2 Framework core: method, metric, plot

## 1. Method

1. This module is developed by **Zhaoyang Huang**
2. New wrapper and method preview, more method will be developed

   |Wrapper|Method|
   |-|-|
   |Direct|PAGA|
   |Linear|Component 1(baseline)|
   |Cycle|Angle(baseline)|
   |Probability|State Component(baseline) |
   |Cluster|Cluster MST(baseline)|
   |Projection|Cluster MST(baseline)|
   |Graph|Graph MST(baseline)|
   |Velocity(TODO)|scVelo(TODO)|
   |Lineage(TODO)|CellRank(TODO)|

3. All methods results are bound to `FateAnnData` by `FateAnnData.add_trajectory_xxx` (xxx is wrapper name).

## 2. Metrics

1. This module is developed by **Haonan Ma**,
2. The usage can been learned by `benchmark.ipynb`.

## 3. Plot

1. This module is developed by **Zhaoyang Huang**
2. Plot raw wrapper `cfe.plot.plot_wrapper`.
3. Web visualization  called `cxgxf` based on cellxgene, The usage can been learned by`notebook_dev/hzy/25.07.19-cellxgene_cellrank_simulation_new_cxgxf.ipynb`. CXGXF Preview: ![Preivew](../img/cxgxf%20preview.gif)

## 4. Developer tools

1. Github workflow: pytest run, docker image run and push automatically.
2. Pre-commit tools is added to retrict black8 programming format and auto pytest.
3. Notebook in development stage should put in `notebook_dev/xxx` (xxx is the developer name). Then, public valid notebook should move to `notebook`.
